/*
 * Generated by BeChicken
 * on 11/13/2019
 * version v1.0
 */
(function () {
    var AviatorPopupController;

    AviatorPopupController = (function () {
        var instance;

        function AviatorPopupController() {

        }

        instance = void 0;

        AviatorPopupController.getInstance = function () {
            if (instance === void 0) {
                instance = this;
            }
            return instance.prototype;
        };

        AviatorPopupController.prototype.setPopupView = function (popupView) {
            return this.aviatorPopupView = popupView;
        };

        AviatorPopupController.prototype.createTopView = function () {
            return this.aviatorPopupView.createTopView();
        };

        AviatorPopupController.prototype.destroyTopView = function () {
            return this.aviatorPopupView.destroyTopView();
        };

        AviatorPopupController.prototype.createHelpView = function () {
            return this.aviatorPopupView.createHelpView();
        };

        AviatorPopupController.prototype.destroyHelpView = function () {
            return this.aviatorPopupView.destroyHelpView();
        };

        AviatorPopupController.prototype.createHistoryView = function () {
            return this.aviatorPopupView.createHistoryView();
        };

        AviatorPopupController.prototype.destroyHistoryView = function () {
            return this.aviatorPopupView.destroyHistoryView();
        };
		
        AviatorPopupController.prototype.createJpHistoryView = function () {
            return this.aviatorPopupView.createJpHistoryView();
        };
		
        AviatorPopupController.prototype.destroyJpHistoryView = function () {
            return this.aviatorPopupView.destroyJpHistoryView();
        };
        return AviatorPopupController;

    })();

    cc.AviatorPopupController = AviatorPopupController;

}).call(this);